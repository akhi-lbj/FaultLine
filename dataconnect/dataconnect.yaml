specVersion: "v1"
serviceId: "faultline"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "faultline-db"
connectorDirs: ["./connector"]
